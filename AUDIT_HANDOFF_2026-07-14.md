# 《往哲荣耀》审查交接清单（2026-07-14）

## 审查结论摘要

- **规则实现完成度：约 90%**。本轮设计范围中的 25 名角色、四大阵营 2/4/6 档、高阶准备快照、六个小羁绊、十波固定路线、商店/合成/出售和存档迁移均已有代码与 Node 回归覆盖。
- **可发布完成度：约 60%**。宽屏页面在最后一次布局调整后尚未取得真实浏览器 DOM 尺寸和人工画面验收；这是一项阻断发布的风险，而不是规则测试失败。
- **当前自动化基线：** `npm.cmd test` 成功；其中 `vinext build` 成功，Node 测试 **47/47** 通过。
- **审查重点：** 先审查布局高度系统和拖放层级，再审查复杂羁绊的非递归保护与存档迁移；不要把 UI 快照问题误判为战斗逻辑回归。

## 已完成：可作为代码审查基线

### 1. 战斗底座与确定性

- 单一 `CombatState`、固定时间步进、`CombatEventQueue`、`StatusManager`。
- 开战冻结 `TraitSnapshot`；成员死亡不重算本波羁绊档位。
- `derivedEffect`、`sourceRouteId`、每波一次 `LastStand`、非递归伤害分摊。
- 延迟技能目标失效兜底、临时阻挡到期释放、罗素拆分守恒。
- 硬控在 6 秒窗口按 100% / 70% / 45% / 25% 递减抗性。
- 十波逐步状态不变量已在 Node 测试覆盖：无 NaN、负生命、负能量、超额阻挡、重复奖励或无效实体 ID。

主要文件：`app/game/combat-core.ts`、`app/game/battle.ts`、`app/game/engine.ts`。

### 2. 内容与经济

- 25 名角色已在角色池中；商店、刷新、经验、购买、出售折价回收、三合一升星、部署/撤回已实现。
- 三条固定路线、地面/高台部署约束、人口、十波与重试检查点已实现。
- 概念地图素材已接入：`app/game/assets/concept-map-v1.png`。

### 3. 四大阵营

- **古希腊 2/4：** 讲席、论辩、三次论辩后的 40% 安全派生技能。
- **德国 2/4/6：** 概念、体系回响、六人绝对体系；个人 50% 回响资格与每波一次限制。
- **法国 2/4/6：** 热度、革命浪潮、街垒/公社、容量、事件队列到期释放、公社消失范围效果。
- **英国 2/4/6：** 证据/定律、按 `sourceRouteId` 的零漏怪实验路线、研究成果、默认力学、政治算术单次领取、两波持续。

### 4. 准备快照与存档

- 可序列化 `PreparationPlan`：讲席、革命节点、实验路线、待选研究、激活研究及领取/持续状态。
- 仅准备阶段可改；开战非法或缺失选择可兜底；战斗中修改不影响本波快照。
- 保存版本为 **5**；旧或不兼容战斗快照安全丢弃，金币、等级、经验、波次、核心、棋子、商店和有效准备配置保留。

### 5. 六个小羁绊

- 辩证法、契约共同体、启蒙、现象学、幸福论、逻辑分析均已接入快照、战斗事件或准备阶段。
- 派生/回响/复制效果被资源与触发入口拦截，避免产生小羁绊资源或再次触发。
- 启蒙议程在准备阶段选择，战斗中冻结。

### 6. 未来扩展接口

- `future-config.ts` 已提供仅数据定义的 `EquipmentDefinition`、`BossModifier`、`WaveSpecialRule` 接口。
- 没有开放装备商店、掉落、装备合成或 Boss 专属技能；这是刻意未实现，不应视为回归。

## 未完成或未验收

### 发布阻断：宽屏战斗页视觉/运行时验收

最后一次布局恢复目标为：根容器三行（84px HUD、`minmax(520px, 1fr)` 主战场、160px Dock），地图与右栏位于主战场，备战与商店位于 Dock 的普通文档流。

仍需真实浏览器在 **1920×1080** 下确认：

1. `mapStage` / `map-field` 实测高度至少 520px、宽高均大于 0。
2. 备战区在地图下方，商店与备战区同属底部 Dock；无 absolute/fixed 叠到地图。
3. Dock 底部与 viewport 底部误差不超过 2px，页面无底部纯黑空带。
4. 地图底图、可部署格、部署单位、敌人与特效仍采用同一相对坐标缩放。
5. 购买、部署、撤回、出售、开波和窗口缩放没有坐标错位。

本会话未能执行这项验收：本地浏览器验证桥初始化失败，报错为 `Cannot redefine property: process`。因此不能以代码推算代替运行时实测。

### 高优先级技术债

- `app/game/drag-safety.css` 经多轮布局覆盖后包含多个级联的桌面媒体查询和 `!important` 规则。最终恢复规则在文件尾部生效，但该样式表应由审查者确认优先级是否仍存在意外覆盖。视觉验收通过后，建议单独安排一次“无行为改动的 CSS 收敛”，不要与机制改动混做。
- `DESIGN_V02.md` 的早期范围描述仍保留“先做纵切、后补 25 人/4/6/小羁绊”的历史措辞，与当前实际进度不完全一致。建议审查后更新文档状态，不要据此误判功能缺失。

### 尚未开始的产品内容（非本轮承诺）

- 装备系统、装备商店、掉落、合成与 UI。
- Boss 专属规则、专属技能、独立首领战设计。
- 正式数值平衡、角色逐个体验调校、完整特效/音频/新手引导。
- 更完整的响应式与可访问性验收、真实浏览器 Console/存档操作验收。

## 建议审查顺序

1. 运行 `npm.cmd test`，确认 47/47 和构建基线。
2. 检查 `GameClient.tsx`：`map-field` 内只能有地图相对坐标内容；`economy-deck` 必须是地图区外的兄弟层。
3. 在浏览器测量上述 1920×1080 几何条件，再手测购买、拖放、撤回、出售、开波与缩放。
4. 审查 `combat-core.ts` / `battle.ts` 对 `derivedEffect`、`redistributed`、`lastStandConsumed`、`sourceRouteId` 的守卫是否在所有入口一致。
5. 审查 `engine.ts` 的保存版本 5 迁移：保留进度，清除不兼容战斗快照，政治算术不重复发钱。
6. 最后以固定阵容跑十波，核对事件队列有界与守恒断言。

## 禁止误改范围

- 不要为修复布局删除或弱化测试。
- 不要回滚小羁绊、25 角色、四大阵营高阶机制、存档迁移或概念地图资源。
- 不要在布局审查中改动战斗坐标、拖放规则、商店经济或角色技能。
