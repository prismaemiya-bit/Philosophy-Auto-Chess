const pagesConfig = {
  base: "/Philosophy-Auto-Chess/",
  root: "static-web",
  publicDir: "../public",
  build: {
    outDir: "../dist-pages",
    emptyOutDir: true,
  },
};

export default pagesConfig;
